<?php
// Set the version components separately.
$apm_version_major = 3;
$apm_version_minor = 2;
$apm_version_patch = 90; // Can be set to null if not used
$apm_version_prepatch = null; // Set to null if a release version.
